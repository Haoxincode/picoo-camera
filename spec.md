可以。下面我把前面讨论的内容收敛成一份可以直接交给设计和开发的 **Pico Camera Receiver Desktop UI 设计规范 v1.0**。这版尽量固定结构，不留太多分叉。

# Pico Camera Receiver Desktop UI 设计规范 v1.0

## 1. 产品定位

Pico Camera Receiver 是电脑端接收程序，用于接收手机 Pico Camera App 的局域网视频流，并将其提供为系统虚拟摄像头。

桌面端的核心用户路径只有一条：

> **打开 Receiver → 手机发现电脑 → 完成连接 → 虚拟摄像头开始输出**

因此整个界面需要保持：

* 单任务感
* 低学习成本
* 状态清晰
* 技术细节默认降级
* 高级配置可访问，但不干扰连接

不采用 Dashboard 式复杂信息架构。

---

# 2. 整体信息架构

整个应用只保留 **一套左侧 Sidebar 导航**。

不增加顶部二级导航。

不在右上角再放 Settings。

最终结构固定为：

```text
Pico Camera

连接

虚拟摄像头
已信任设备
网络
通用


帮助
关于
```

推荐顺序：

1. 连接
2. 虚拟摄像头
3. 已信任设备
4. 网络
5. 通用
6. 帮助
7. 关于

其中：

* `连接` 是默认首页。
* `帮助`、`关于` 固定在 Sidebar 底部。
* 不设置独立的「状态」页面。
* 所有状态直接显示在对应功能页面中。
* 不设置一个笼统的「设置」页面。

---

# 3. 应用整体布局

桌面端推荐最小窗口：

**1180 × 720**

推荐默认尺寸：

**1440 × 860 左右**

结构：

```text
┌──────────┬────────────────────────────────────┐
│          │                                    │
│ Sidebar  │            Content                 │
│          │                                    │
│          │                                    │
│          │                                    │
└──────────┴────────────────────────────────────┘
```

## Sidebar

推荐宽度：

**200px**

允许范围：

**192–216px**

固定占满窗口高度。

内容区域独立滚动，Sidebar 不跟随滚动。

---

# 4. Sidebar 规范

## 4.1 品牌区

顶部：

```text
◉  Pico Camera
```

建议高度：

**72px**

Logo：

* 24×24 或 28×28
* 蓝色品牌色
* 与文字间距 10–12px

产品名称：

* 15–16px
* Medium / Semibold

---

## 4.2 一级导航

每个 Navigation Item：

* 高度：44px
* 左右 padding：12px
* 圆角：8px
* Icon：18–20px
* Icon 与文字间距：10px
* 文字：14px

正常状态：

```text
icon  文本
```

Active：

* 背景使用低透明度品牌蓝
* Icon / Text 使用较亮蓝色或白色
* 不使用大面积发光

建议：

```text
▣ 连接
◉ 虚拟摄像头
▣ 已信任设备
⌁ 网络
⚙ 通用
```

不要增加：

* 状态
* Dashboard
* 首页
* Receiver 设置

这些都会制造无意义层级。

---

## 4.3 底部区

Sidebar 底部固定：

```text
? 帮助
ⓘ 关于
```

与主要功能之间保持明显留白。

帮助属于低频行为，因此不要提高视觉权重。

---

# 5. 视觉体系

整体保持当前深色方向。

## 基础颜色层级

### Background

应用背景：

```text
#080D16 ～ #0B1220
```

Sidebar：

略深于内容背景。

### Surface

一级内容区域：

```text
#101827
```

二级 Surface：

```text
#131D2D
```

边框：

低对比度蓝灰。

---

## 强调色

### Brand / Action

蓝色仅用于：

* Primary Button
* 当前导航
* 选中项
* 连接码关键视觉
* 可交互链接

### Success

绿色仅用于：

* 已就绪
* 已连接
* 在线
* 正常

### Warning

黄色 / 橙色仅用于：

* 需要用户注意
* 降级状态
* 网络异常

**IP 地址不使用橙色。**

### Danger

红色仅用于：

* 撤销信任
* 删除
* 错误状态
* 破坏性操作

---

# 6. Typography

建议采用系统字体：

Windows：

```text
Segoe UI
```

macOS：

```text
SF Pro
```

中文：

系统默认中文字体即可。

推荐层级：

### Page Title

22–24px / Semibold

### Section Title

15–16px / Semibold

### Setting Title

14px / Medium

### Body

13–14px

### Description

12–13px

### Metadata

12px

### Connection Code

48–64px

数字最好使用：

* Tabular Numerals
* SemiBold

---

# 7. 页面一：连接

这是应用的核心页面。

所有重要信息围绕：

> **手机如何连上这台电脑**

展开。

推荐桌面布局：

```text
┌────────────────────┬──────────────────────┐
│                    │                      │
│ Studio PC           │ 设备与连接           │
│                    │                      │
│ 482 917            │ iPhone 16 Pro       │
│                    │      [连接]          │
│ IP                  │                      │
│                    │ 手机 → PC            │
│ 状态                │                      │
│                    │ 开始使用             │
│                    │                      │
│                    │ 网络状态             │
└────────────────────┴──────────────────────┘
```

推荐比例：

**左侧 55% / 右侧 45%**

---

## 7.1 Receiver 信息

标题：

> **Studio PC（本机）**

副标题：

> Pico Camera 接收端

可以配一个显示器图标。

不要在这里展示：

> QUIC Receiver
> UDP Server
> 4433 Listening

这些都属于内部实现。

---

## 7.2 连接码

标题：

> 连接码

主体：

```text
482 917
```

说明：

> 手机输入连接码后即可连接到这台电脑

连接码是页面第一视觉焦点。

推荐：

* 高度 120–150px
* 字号 56px 左右
* 两组三位数字
* 中间使用自然空格
* 可以使用轻微蓝色边框 / Glow
* 不要产生游戏 HUD 感

---

## 7.3 IP 直连

作为备用方案存在。

```text
192.168.1.108:4433        ⧉
```

不需要强调「备用」。

鼠标悬停后：

> 复制连接地址

复制成功：

> 已复制

---

## 7.4 基础状态

连接页面只显示三个对用户真正有价值的状态：

```text
虚拟摄像头                         就绪 ✓
自动发现                           已开启 ✓
连接方式                           局域网直连 ✓
```

建议使用 Setting Row。

不要做成三个按钮。

---

## 7.5 设备与连接

右栏第一优先级。

### 未发现设备

```text
设备与连接

等待手机连接

打开 Pico Camera App 后
将自动发现这台电脑
```

### 已发现设备

```text
iPhone 16 Pro

● 已发现 · 等待连接

                               [连接]
```

### 已连接设备

```text
iPhone 16 Pro

● 已连接

1080p · 30 FPS · 12 ms

                               [断开]
```

也就是说右栏本身承担完整状态机：

```text
等待设备
    ↓
发现设备
    ↓
连接
    ↓
正在推流
```

---

# 8. 页面二：虚拟摄像头

页面标题：

> **虚拟摄像头**

副标题：

> 管理系统虚拟摄像头和无视频流时的输出画面

页面不采用 Dashboard 卡片堆叠。

采用：

> Section + Row

---

## 8.1 驱动状态

Section：

> 系统虚拟摄像头

内容：

```text
Picoo Camera                           ● 正常

腾讯会议、Zoom、Teams、OBS 等应用可以直接使用。
```

如果正常：

不提供操作按钮。

如果异常：

```text
Picoo Camera                           ! 异常

                                  [重新检测]
```

如果驱动不存在：

```text
尚未安装虚拟摄像头

                                  [安装驱动]
```

---

# 9. 无视频流画面

Section：

> 无视频流画面

说明：

> 手机未连接或视频中断时显示的内容

提供三个 Visual Option：

```text
┌──────────────┐
│  Picoo       │
│  Camera      │
└──────────────┘
品牌画面 ✓


┌──────────────┐
│              │
│    BLACK     │
└──────────────┘
纯黑画面


┌──────────────┐
│ SMPTE BARS   │
│ ▉ ▉ ▉ ▉ ▉   │
└──────────────┘
测试彩条
```

要求：

* 有真实 Preview
* 卡片宽约 160–180px
* 16:9
* Selected 使用蓝色 Border
* 卡片本身可点击

不使用单纯的 Radio。

---

# 10. 视频输出信息

只读能力不需要伪装成设置。

建议：

```text
视频输出

最大分辨率                         1920 × 1080
最大帧率                           30 FPS
```

如果当前版本完全不可配置，也可以压缩成：

> 最大输出：1080p · 30 FPS

---

# 11. 页面三：已信任设备

标题：

> **已信任设备**

副标题：

> 已完成验证，可以直接连接的手机

避免：

> 双向核对
> Pairing Identity
> Trusted Peer

这些安全实现术语。

---

## 11.1 Device List

设备卡：

```text
┌────────────────────────────────────────────┐
│ 📱 Xiaomi 14 Ultra                     ··· │
│                                            │
│     刚刚连接                               │
│                                            │
│     192.168.1.108      a83b:19c0:77e2      │
└────────────────────────────────────────────┘
```

设备名称优先。

第二信息：

> 最近连接时间

第三信息：

* IP
* 简化后的 Device ID

不要把 Metadata 放得比设备名称更显眼。

---

## 11.2 Device Menu

点击 `···`：

```text
查看详情
────────────────
撤销信任
```

撤销信任为 Danger。

确认 Dialog：

### 撤销对 Xiaomi 14 Ultra 的信任？

撤销后，该设备下次连接时需要重新输入连接码。

Buttons：

```text
取消      撤销信任
```

---

# 12. 页面四：网络

标题：

> **网络**

副标题：

> 配置局域网发现与连接服务

不使用：

> 网络与设备发现

减少语言复杂度。

---

# 13. 自动发现

```text
自动发现附近设备                        [ON]

允许同一局域网中的 Pico Camera
自动发现这台电脑。
```

默认：

**ON**

关闭后：

手机需要手动输入：

* 连接码
* 或 IP

---

# 14. 高级网络参数

以下内容属于高级设置。

可以做：

```text
高级                                     ˅
```

展开后显示。

## 监听端口

标题：

> 监听端口

Input：

```text
4433
```

Description：

> 视频与控制连接使用的 UDP 端口

建议校验：

```text
1024–65535
```

避免冲突提示：

> 该端口已被其他程序占用。

---

# 15. 连接码有效期

标题：

> 连接码有效期

Select：

```text
5 分钟                      ▾
```

建议选项：

* 1 分钟
* 5 分钟
* 10 分钟
* 30 分钟

默认：

> 5 分钟

说明：

> 到期后自动生成新的连接码。

UI 中不出现：

> 防重放
> Replay Attack
> Token TTL

---

# 16. 页面五：通用

标题：

> **通用**

---

# 17. 电脑名称

```text
电脑名称

┌─────────────────────────────────┐
│ Studio PC                       │
└─────────────────────────────────┘

手机上看到的这台电脑名称。
```

字段名称使用：

> **电脑名称**

不用：

> Device Name
> Receiver Name
> 广播名称

推荐限制：

* 1–32 字符
* Trim 首尾空格
* 允许中文
* 允许英文
* 允许数字
* 允许常规符号

---

# 18. 后台运行

```text
关闭窗口后继续在后台运行              [ON]

保持手机连接和虚拟摄像头可用。
```

默认：

建议 ON。

不要在普通 UI 中写：

> 保持 QUIC Server 运行

---

# 19. 系统托盘行为

如果开启后台运行：

关闭主窗口：

> 最小化到 Tray

Tray Menu：

```text
打开 Pico Camera
────────────────
连接状态
────────────────
退出
```

真正退出软件必须通过：

> 退出

---

# 20. 页面六：关于

标题：

> **关于**

页面主体建议居中。

```text
        Logo

Pico Camera Receiver

局域网低延迟无线手机摄像头

Version 1.0.0
Build 2026.08.29
```

下面：

```text
检查更新
开源许可
隐私政策
```

如果没有自动更新机制：

不要显示：

> 检查更新

---

# 21. 帮助

Help 不需要复杂的内部页面。

可以有：

```text
快速开始
连接问题
虚拟摄像头问题
网络问题
```

或者直接打开官方帮助网站。

Sidebar 中它仍然保留为低优先级入口。

---

# 22. Setting Page 通用布局规范

所有配置页统一：

```text
Page Title
Description


Section Title
──────────────────────────

Setting Row


Setting Row


Section Title
──────────────────────────

Setting Row
```

## Content Width

建议：

**720–820px**

不要让设置项铺满整个 1440px 屏幕。

页面左对齐。

推荐：

```text
Content Left Padding = 40px
Content Top Padding = 36px
```

---

# 23. Setting Row

标准 Toggle Row：

```text
标题                                   [ON]
辅助说明
```

高度：

**64–72px**

标题：

14px Medium

说明：

12–13px

Toggle：

靠右。

---

# 24. Section

Section 间距：

**32–40px**

Section Title：

15px / Semibold

标题与第一行：

16px

同 Section 行间距：

0–8px。

---

# 25. Button 体系

整个应用尽量只使用三种 Button。

## Primary

例如：

> 连接

蓝色实心。

高度：

36px。

---

## Secondary

例如：

> 重新检测

Dark Surface + Border。

---

## Destructive

例如：

> 撤销信任

尽量在 Menu / Confirmation 中使用。

不常驻巨大红色按钮。

---

# 26. Icon 体系

推荐统一使用一个 Icon Library。

例如：

* Lucide
* Phosphor
* Fluent Symbols

推荐 Lucide。

标准大小：

```text
Sidebar    18–20
Row        18
Status     16
Page icon  20–24
```

Stroke 保持一致。

不要混用：

* Filled icons
* 3D icons
* Emoji
* Outline icon

---

# 27. Status 规范

统一表达：

### 正常

```text
● 正常
```

绿色。

### 等待

```text
● 等待连接
```

蓝灰。

### Warning

```text
● 需要处理
```

黄色。

### Error

```text
● 异常
```

红色。

不要同一页面同时混用：

* 小圆点
* Check
* Badge
* Pill
* Icon

建议主要统一为：

> **Dot + Text**

---

# 28. Terminology 规范

最终建议固定：

| 技术概念                  | UI 文案                |
| --------------------- | -------------------- |
| Receiver              | Pico Camera Receiver |
| Device Name           | 电脑名称                 |
| Discovery Broadcast   | 自动发现                 |
| UDP Listen Port       | 监听端口                 |
| Pairing Code          | 连接码                  |
| Trusted Device        | 已信任设备                |
| Virtual Camera Driver | 虚拟摄像头                |
| Fallback Screen       | 无视频流画面               |
| QUIC Connection       | 局域网连接                |
| Replay Protection TTL | 连接码有效期               |

原则：

> **UI 描述用户能感受到的概念，日志和诊断描述底层技术。**

---

# 29. 不应该出现在普通设置中的术语

建议避免：

* QUIC
* mDNS
* UDP Socket
* Replay Attack
* TTL
* Driver Extension ID
* Certificate Fingerprint
* Peer Identity
* Control Channel

这些可以出现在：

> 高级诊断 / Logs

以后如果真的需要再增加。

---

# 30. 首页与设置页的视觉差异

这是整个设计里很重要的一条。

### 连接首页

可以：

* 大数字
* Illustration
* Glow
* Device Card
* 状态图形化

偏：

> Product Experience

### 设置页面

应该：

* 安静
* 清晰
* 大留白
* Row-based
* 少 Card

偏：

> System Settings

不要所有页面都做成首页那种仪表盘。

---

# 31. 空状态

已信任设备为空：

```text
暂无已信任设备

通过连接码连接手机后，
设备会出现在这里。
```

不要显示一个空 Table。

---

# 32. Loading

自动发现期间：

```text
正在搜索附近设备…
```

可以使用轻量 Spinner。

超过数秒后：

```text
暂未发现设备

请确认手机和电脑位于同一局域网。
```

提供：

> 重新搜索

---

# 33. Error 示例

## 网络不可用

```text
无法使用局域网连接

请检查电脑的网络连接。
```

## 端口冲突

```text
监听端口 4433 已被占用。

更改端口后重新启动连接服务。
```

## 虚拟摄像头异常

```text
虚拟摄像头不可用

第三方应用暂时无法使用 Picoo Camera。

[重新检测]
```

错误提示：

* 说明发生了什么
* 说明影响
* 给出一个动作

不要只显示：

> Error 105

---

# 34. 最终页面结构

最后固定为：

```text
Pico Camera
│
├─ 连接
│
├─ 虚拟摄像头
│   ├─ 驱动状态
│   ├─ 无视频流画面
│   └─ 视频输出
│
├─ 已信任设备
│
├─ 网络
│   ├─ 自动发现
│   └─ 高级
│       ├─ 监听端口
│       └─ 连接码有效期
│
├─ 通用
│   ├─ 电脑名称
│   └─ 后台运行
│
├─ 帮助
│
└─ 关于
```

这套 IA 已经足够支撑当前版本以及后面一段时间的扩展。

---

# 35. 一条最重要的设计原则

这款软件应该让用户形成这样的感觉：

> **打开以后基本不用配置。**

正常使用时，用户长期停留在：

> **连接**

其他五个页面都是偶尔才进入。

所以后续如果出现一个新功能，优先判断：

> 它能不能自动工作？

其次才是：

> 它应该放哪个设置页面？

这样可以避免 Receiver 慢慢变成一个工程师控制台。

我建议这版直接作为 **Desktop v1.0 UI / UX 基线规范**，后续开发就围绕这套 IA 和组件规范收敛，不再继续增加一级入口。
