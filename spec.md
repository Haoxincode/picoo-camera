# Pico Camera Receiver Desktop UI 设计规范 v2.0

> **状态**：`Implemented & Aligned`  
> **对应原型**：[`pico-camera-receiver.html`](./pico-camera-receiver.html)  
> **关联架构**：[ARCH-PICOO-UI-001](docs/design-specs/architecture/0009-desktop-gpui-mobile-native-ui-boundary.md)  
> **关联需求**：[REQ-PICOO-UI-0001](docs/design-specs/requirements/req-picoo-ui-0001-native-camera-and-desktop-gpui-acceptance.md)

---

## 1. 产品定位与核心设计原则

Pico Camera Receiver 是电脑端接收程序，用于接收手机 Pico Camera App 的局域网视频流，并将其提供为系统虚拟摄像头。

桌面端的核心用户路径只有一条：

> **打开 Receiver → 手机发现电脑 → 点击连接 → 虚拟摄像头开始输出**

### 核心设计原则
1. **极致单任务感**：待机时专注“如何连接”，推流时聚焦“画面监视与镜头控制”，杜绝仪表盘（Dashboard）式的散乱信息。
2. **零冗余与概念收敛**：
   - 彻底废除独立的“已信任设备”一级导航，将多设备管理直接收敛于「连接」主页的设备列表中；
   - 彻底移除冗余的“系统状态”卡片，统一收归为极简的 4 行网络状态与 3 项系统指示；
   - 移除主机标题中生硬的 `（本机）` 括号文字，采用轻量化状态徽标与 PC 图标表达。
3. **视觉高度稳定与饱满**：
   - 左右采用 `58% : 42%` 黄金分栏结构；
   - 右侧待机时采用多设备独立卡片构成的纵向滚动列表，消除竖版界面的空旷感；
   - 推流时动态收起列表，平滑展开视频规格指标与镜头控制网格。
4. **轻量连接路径**：不提供二维码生成或扫码入口，不引入扫码解析 SDK。正常连接依赖 mDNS 自动发现；mDNS 失效时使用 `IP:端口` 定位 Receiver，六位连接码仅承担短期配对授权。

---

## 2. 整体信息架构

整个应用只保留 **一套左侧 Sidebar 导航**，无顶部二级导航与全局弹窗设置。

最终架构收敛为：

```text
┌─────────────────────────┐
│ ◉ Pico Camera           │
├─────────────────────────┤
│ ▣ 连接                  │ ← 默认首页（主机标识/连接码/开始使用/设备管理/网络）
│ ◉ 虚拟摄像头            │ ← 虚拟驱动状态检测、注册与一键修复
│ ⌁ 网络                  │ ← 局域网传输接口、端口监听与网络诊断
│ ⚙ 通用                  │ ← 偏好设置、设备重命名与 GPU 解码配置
├─────────────────────────┤
│ ? 帮助                  │ ← 常见问题与排障指引
│ ⓘ 关于                  │ ← 版本信息与开源许可
│ ☼ 主题切换              │ ← 深色 / 浅色模式一键切换
└─────────────────────────┘
```

---

## 3. 窗口与布局规范

* **推荐最小窗口尺寸**：`1180 × 720 px`
* **推荐默认窗口尺寸**：`1440 × 860 px`
* **Sidebar 宽度**：`204 px` 固定
* **主工作区（Content Area）**：占据剩余宽度，独立滚动，保持响应式弹性伸缩。

```text
┌──────────┬─────────────────────────────────────────────────┐
│          │ [58% 左栏: 主机与指引 / 实时视频监视]           │
│ Sidebar  │                                                 │
│ (204px)  ├─────────────────────────────────────────────────┤
│          │ [42% 右栏: 设备连接状态机 + 极简网络状态]       │
└──────────┴─────────────────────────────────────────────────┘
```

---

## 4. 视觉体系与色彩令牌

全局采用 **Dark Slate Industrial** 现代暗色工业质感设计语言，基于 Tailwind CSS v4 与 OKLCH 色彩体系：

### 4.1 色彩令牌映射
* **Background（应用底色）**：`oklch(0.145 0 0)`（深灰黑）
* **Card Surface（一级容器）**：`oklch(0.205 0 0)`，边框 `oklch(1 0 0 / 10%)`
* **Secondary Surface（二级卡片）**：`oklch(0.274 0.006 286.033)`
* **Primary / Accent（品牌强调色）**：`oklch(0.424 0.199 265.638)`（科技蓝）
* **Status-Success（在线/就绪）**：`oklch(69.6% 0.17 162.48)`（翠绿 `#3ecf8e`）
* **Status-Danger（断开/危险）**：`oklch(0.704 0.191 22.216)`（柔红 `#ef4444`）

### 4.2 字体排版
* **主要字体**：Inter / SF Pro / PingFang SC
* **等宽字体（必须强制用于连接码与遥测）**：`JetBrains Mono`（支持数字等宽与字间距 `letter-spacing: 0.22em`）

### 4.3 矢量图标标准
* 全局 100% 采用 **[Reicon](https://github.com/dqev/reicon)**（24×24 标准网格，2px 纯净描边与几何曲率），严禁在界面中使用系统 Emoji。

---

## 5. 核心页面深度规范：页面一「连接」（Connect）

### 5.1 左侧大卡片（58% 宽度 · 主机标识、配对与推流大屏）

#### A. 待机状态（Idle / Pairing View）
1. **主机识别卡片（Top Header）**：
   - 左侧：圆形/圆角电脑图标（Monitor Icon）；
   - 主标题：**`Studio PC`**（精简纯粹，**彻底移除 `（本机）` 括号文字**）；
   - 状态徽标：`<span class="w-1.5 h-1.5 rounded-full bg-emerald-500"></span> 接收端已就绪`；
   - 副标题：`局域网无线视频接收与虚拟摄像头输出`。
2. **大号 6 位连接码（Hero Area）**：
   - 居中等宽大字号（`482 917`，`56-62px`），点击支持一键刷新生成新短码；
   - 辅助文字：`手机输入连接码后即可连接到这台电脑`；
   - 备用 IP 直连胶囊：`192.168.1.108:4433`（附一键复制按钮）。
   - 连接码不承担网络地址解析：自动发现可用时由 mDNS 提供 Endpoint；自动发现不可用时，手机端先输入上方 IP 地址，再提交连接码完成授权。
3. **真机连接拓扑与开始使用（Bottom Area）**：
   - **开始使用 3 步指引**：
     1. 📱 `打开手机 App`：确保手机与电脑处于同一局域网 Wi-Fi；
     2. 🔗 `选择此电脑`：点击自动发现的本电脑；发现失败时输入桌面端展示的 IP 地址；
     3. 📹 `连接并开始推流`：输入 6 位连接码完成授权，在会议软件中选择 Pico Camera 虚拟摄像头。
   - **真机硬件互联拓扑图**：
     - 左侧手机（`iPhone 16 Pro`） ➔ 中间动态呼吸无线波纹（`Wi-Fi 5GHz · 低延迟`） ➔ 右侧电脑（`Studio PC`）。
   - **3 项轻量系统状态**：
     - 📹 `虚拟摄像头：就绪 ✓`
     - 📡 `自动发现服务：在线 ✓`
     - 🛡️ `连接方式：局域网直连 ✓`

#### B. 推流状态（Streaming Live View）
1. **推流头部**：`iPhone 16 Pro` 正在推流至 `Studio PC`；
2. **实时大屏视频流监视器**：
   - 16:9 画幅视频画面渲染；
   - 左上角实时状态水印：`● LIVE · 1080p 60FPS · H.265`；
   - 右上角虚拟摄像头输出状态：`VIRTUAL CAMERA: ACTIVE`。

---

### 5.2 右侧分栏（42% 宽度 · 设备连接状态机与极简网络状态）

#### A. Box 1: 设备与连接（Device State Machine）
* **待机状态（未连接）**：
  * 标题旁徽标：`3 台在线 · 可选连接`；
  * **多设备独立卡片纵向滚动列表**：
    - `iPhone 16 Pro`（主力 · 主摄 48MP · 局域网在线 · `[ 连接 ]` 按钮）
    - `iPad Pro 11"`（超广角 12MP · 待机在线 · `[ 连接 ]` 按钮）
    - `Xiaomi 14 Ultra`（徕卡 50MP · 待机在线 · `[ 连接 ]` 按钮）
    - `MacBook Air M2`（无线副摄 · 离线）
  * 容器支持平滑纵向滚动，保持竖版视觉饱满，杜绝空旷感；
  * 底部提供 `发现受信任设备时自动连接` 开关。
* **推流状态（已连接）**：
  * 多设备列表自动收起；
  * 原地切换为**单一条目推流设备卡片**（显示 `实时推流中 · 1080p 60FPS` 及红色危险态 `[ 断开 ]` 按钮）；
  * **展开实时视频规格参数**：
    - 视频规格：`1080p · 60 FPS`（支持点击快捷轮换 4K / 1080p / 720p）；
    - 解码与码率：`H.265 HW · 8.4 Mbps`；
    - 传输缓冲/抖动：`8ms · Jitter 0.2ms`；
    - 手机电量：`88% · 充电中`。
  * **展开 4 组镜头画面控制网格**：
    1. ⇋ **`镜像翻转`**（水平镜像翻转）；
    2. 📷 **`镜头切换`**（前置 / 后置广角 / 超广角）；
    3. 🔄 **`画面修复`**（请求 IDR 关键帧消除卡顿花屏）；
    4. 📸 **`拍照截图`**（截取当前高清帧画面）。

#### B. Box 2: 网络状态（Network Status）
保持极简 4 行原生指标，清晰透出局域网健康状况：
* `网络`：局域网可用 ✓
* `发现服务`：在线 ✓
* `延迟`：低 ✓
* `安全`：已保护 ✓

---

## 6. 其他页面规范简述

* **页面二：虚拟摄像头（Virtual Camera）**：
  - 核心展示虚拟摄像头驱动状态（`就绪 / 需安装 / 需修复`）；
  - 提供系统扩展安装、卸载与重新注册操作；
  - 提供第三方应用（OBS、腾讯会议、飞书、Zoom）识别指引。
* **页面三：网络（Network）**：
  - 展示局域网 IPv4 / IPv6 地址、mDNS 广播状态与 UDP QUIC 4433 端口配置；
  - 提供网络诊断日志导出功能。
* **页面四：通用（General）**：
  - 开机自启、关闭时最小化到系统托盘；
  - 接收端设备名称重命名（`Studio PC`）；
  - GPU 硬件解码加速开关与日志级别配置。
* **页面五：帮助（Help）与 页面六：关于（About）**：
  - 常见问题排查（防火墙放行、Wi-Fi 隔离排查）；
  - 版本号、开源协议与技术支持入口。

---

## 7. 架构演进与决策变更记录

| 日期 | 变更内容 | 决策依据与动机 |
| :--- | :--- | :--- |
| **2026-08-29** | **移除 Sidebar「已信任设备」一级导航** | 信任设备管理已直接融合在「连接」页多设备滚动列表中，无需独立页面冗余分散用户注意力。 |
| **2026-08-29** | **移除右侧独立的「系统状态」卡片** | 该卡片内容与左侧 3 项指示及右侧「网络状态」高度重复，移除后结构大幅清晰。 |
| **2026-08-29** | **重构「连接」页分栏（左 58% / 右 42%）** | 将开始使用与拓扑图移至左侧大卡片，右侧 Box 1 专注设备连接状态机，Box 2 专注网络状态。 |
| **2026-08-29** | **优化主机识别 Header** | 移除生硬的 `（本机）` 括号文本，采用绿色状态徽标与 PC 图标，符合现代 Apple 级设计美学。 |
| **2026-08-29** | **移除二维码生成与扫码连接** | 自动发现、手动 `IP:端口` 与六位连接码已覆盖局域网连接和配对；不引入扫码 SDK，避免增加移动端包体积与相机权限路径。 |
