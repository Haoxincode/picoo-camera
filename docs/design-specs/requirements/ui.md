# REQ-PICOO-UI：手机原生 UI + 桌面 GPUI

| ID | 状态 | 来源 | 描述 | 验收 |
| --- | --- | --- | --- | --- |
| REQ-PICOO-UI-001 | implemented | ARCH-PICOO-UI-001 | GPUI View 不直接持有 QUIC/Decoder/Frame Buffer | `ReceiverRuntime` + `ReceiverSnapshot`；View 只观察 |
| REQ-PICOO-UI-002 | implemented | PRD §16 / REQ-PICOO-UI-0001 AC-D-ONBOARDING-03、AC-D-NAV-02、AC-D-NAV-03 | 桌面首次启动/连接/虚拟摄像头/网络/通用/帮助/关于导航；桌面首次启动默认亮色并可切换深色主题；工作区使用贴边单层结构，Sidebar 可在 204px 文案态与 48px 图标态之间切换，折叠控制与平台窗口按钮共用顶部工具行；连接与虚拟摄像头分别使用 `monitor-phone` / `monitor-camera` Reicon，折叠控制使用 Reicon Filled `sidebar-left` / `sidebar-right`；等待页显示手动连接地址，并使用 HTML 原型同源的 iPhone 与 MacBook 真机 SVG 表达手机→无线→电脑拓扑；Windows 产品启动不附带控制台，普通启动不触发需管理员权限的 VCam 修复；日志级别运行时可 reload | `DesktopPage` + `DesktopSection`；`sidebar_collapsed` 进程内视图状态；`TitleBar` 作为主内容顶部工具行的窗口行为容器，Windows 窗口按钮位于同一行最右侧，macOS 交通灯保留 Sidebar 安全区，且两端不显示重复品牌标题；默认 `ThemeMode::Light`，主题按钮可切换 `Picoo Light` / `Picoo Dark`；折叠按钮及图标按钮具备稳定 ID、tooltip 与 accessibility label；主内容区具备完整 flex/scroll 尺寸链；`hardware_topology` + 内嵌 `assets/device-frames/` 多色 SVG（MacBook 深浅主题）；`hardware_topology_svgs_are_embedded_with_intrinsic_dimensions`；`desktop_semantic_reicons_are_valid_svg_assets`；Windows GUI PE subsystem 门禁；VCam 检测不启动外部控制台进程，普通启动只调用无注册表写入能力的 `start_registered_on_worker`；显式修复按钮具备 loading/成功/失败反馈；`render_manual_endpoint_card`；改名→mDNS；`logging.rs` EnvFilter reload |
| REQ-PICOO-UI-003 | implemented | PRD §17 / PUC-008 | 手机设备列表、手动 IP 直连、配对、传输页；Compose 可渲染状态与编码器事务状态分离 | Devices 原生分组行；Android/iOS 原生 Manual Connect Sheet（输入错误保留 Sheet）；Pairing/Streaming 显示 Receiver 名；Cancel 取消配对；Android `SenderSessionViewModel` 跨配置变更持有 Rust/Encoder 会话，设置持久化；`SenderHomeState` + 非 Compose `EncoderReconfigurationCoordinator` |
| REQ-PICOO-UI-004 | implemented | ARCH-PICOO-UI-001 | VideoSurface 只渲染纹理，不拥有解码器；Live 显示 Network Quality | `video_surface.rs`；`network_quality.rs` 与 Android `LinkQuality` 阈值对齐 |
| REQ-PICOO-UI-005 | implemented | PUC-005 | Sender 前台、防锁屏、深色低亮度、过热/低电量提示；API33+ 通知权限 | FGS + `POST_NOTIFICATIONS` + `FLAG_KEEP_SCREEN_ON` + Streaming `screenBrightness` 调暗 + `PowerHints`；`ManifestPermissionsTest`（真机验证仍待） |
| REQ-PICOO-UI-006 | implemented | PUC-001 / PUC-008 | 权限在操作时请求，非启动一次性弹出；连接流程不请求相机权限 | 开始视频采集时请求 CAMERA；Nearby/Notifications 按需；`ManifestPermissionsTest` |
| REQ-PICOO-UI-007 | implemented | PRD §16 | 开机启动偏好写入 OS | `startup.rs` HKCU Run（Windows）+ MemoryStore 测试 |
| REQ-PICOO-UI-008 | implemented | PRD §16 | Windows 关闭窗口时可最小化到系统托盘；macOS 不伪装 Windows 托盘，关闭后保留在 Dock/后台 | Windows `NotifyIconController` + tip 随 `ReceiverStatus`；`TrayMenuAction::apply` Show/Quit；message-only HWND 或 `FindWindowW`；macOS 构建不包含 Notify Icon 状态 |
| REQ-PICOO-UI-009 | implemented | PUC-005 | 桌面直播页远程摄像头控制（前后摄 / 480p·720p·1080p / 远端镜像）经 ReceiverSession → CameraCommand；Sender FFI/JNI 消费 | GPUI Live 前置/后置/480p/720p/1080p/远端镜像 → `send_camera_command`；`picoo_sender_take_camera_command` + Android poll |
| REQ-PICOO-UI-010 | implemented | ARCH-PICOO-UI-001 | iOS Sender 使用 SwiftUI 复现设备列表、手动连接、配对与传输页面，不复制 Rust 业务状态机 | `SenderAppModel` 以 Observation 单向驱动设备/手动连接/配对/等待/传输页；状态来自 Rust C ABI，Camera 权限只在进入传输页时请求；原生 Form 持久化自动直连和默认画质，自动选择最近连接且当前在线的信任设备；等待页区分拒绝/超时，直播页显示重连退避并允许停止；撤销信任需二次确认；Reicon 使用类型安全的本地 SVG Asset Catalog；完整真机交互仍按 `REQ-PICOO-UI-0001` 验收 |
| REQ-PICOO-UI-011 | planned | ARCH-PICOO-UI-001 | macOS Receiver 复用 `apps/desktop` 的 GPUI 页面与主题，仅隔离启动项、窗口后台行为、虚拟摄像头状态等平台适配 | `xtask build macos` 编译同一 GPUI View；Finder/Dock/`open` 启动 `.app` 能及时显示主窗口，Shared Frame Ring 文件访问使用非阻塞打开语义，无签名包由独立 Info.plist marker 选择 Application Support 降级路径；macOS 不构建 Win32 Notify Icon；Camera Extension 使用 `Bundled / 待激活`、系统查询 Active、重启后生效和停用状态，pending intent 记录 boot session，重启未收敛时解除锁并允许重试；激活/查询在后台执行且不显示 MSI/COM 文案；推流不得冒充扩展已激活；不新增 macOS 专属 WebView/SwiftUI 桌面 UI；仍需签名真机交互验收 |
| REQ-PICOO-UI-012 | planned | ARCH-PICOO-UI-002 / REQ-PICOO-UI-0001 | 全端共享 Primitive→Semantic→Component 设计契约与 Reicon 语义清单；移动 Control context 跟随系统主题，Camera context 独立深色；Compose、SwiftUI、GPUI 保留原生交互并通过平台 adapter 消费同一语义 | 已落地 Android `PicooTheme`/原生 TopAppBar、IME-safe Sheet、单节点 Switch 语义、CameraStop/Motion token、Phone/Foldable/Tablet Preview 与 API 35 edge-to-edge；iOS Any/Dark Color Set、SwiftUI token、Navigation/Form/Sheet、Light/Dark + Dynamic Type Preview；`assets/icons/reicon/manifest.json` 与双端类型安全语义 adapter；`check_reicon_assets.sh` 同时校验文件和 semantic→glyph 映射，Compose 语义测试覆盖自动直连开关。REQ 保持 planned，直到 GPUI 语义 adapter、代表截图与移动真机无障碍/旋转验收完成 |
| REQ-PICOO-UI-013 | implemented | ARCH-PICOO-UI-001 / PRD §10、§16 | 产品 App Icon 作为独立品牌资产，不复用 Reicon 功能图标；Windows EXE、开始菜单、托盘和已安装应用列表，以及 macOS Finder、Dock、Launchpad 与 Spotlight 使用同一品牌家族，Windows 托盘使用小尺寸简化派生图标；macOS 继续使用 Dock/后台生命周期，不因图标接入伪装 Windows 托盘 | `assets/brand/app-icon-master.svg` + `tray-icon-master.svg` 为矢量事实源；`cargo xtask generate brand-icons --check` 校验 `.ico/.icns` 派生产物；Windows PE 资源包含 16/24/32/48/256 App Icon 与 16/20/24/32/40/48/64 Tray Icon，`Shell_NotifyIconW` 设置 `NIF_ICON` 并使用版本 4 事件；WiX `ARPPRODUCTICON`；macOS `Contents/Resources/PicooCamera.icns` + `CFBundleIconFile`；`xtask` 图标尺寸测试与平台 bundle 门禁 |
