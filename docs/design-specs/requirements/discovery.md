# REQ-PICOO-DISCOVERY：设备发现

| ID | 状态 | 来源 | 描述 | 验收 |
| --- | --- | --- | --- | --- |
| REQ-PICOO-DISCOVERY-001 | implemented | PUC-002 | Receiver 广播 `_picoocam._udp.local`，广播出口必须绑定所选物理局域网地址，不得漂移到 VPN/utun/WireGuard/Docker/Hyper-V/WSL 等隧道或虚拟接口；桌面只有收到 mDNS daemon 的实际 Announce 后才能显示发现服务在线；Windows 新装与从旧版升级都必须允许局域网 mDNS 流量 | `select_advertise_ipv4_with_interfaces` 排除虚拟/隧道接口且无物理 LAN 时不回退到 VPN/loopback；MdnsAdvertiser 注册/改名/信任变更后 re-advertise，并以 `IfKind::All` disable + LAN IP enable 限定 mDNS daemon；`DaemonEvent::Announce`/`Error`/`IpDel` 驱动真实状态；WiX 为 mDNS UDP `5353` 使用独立新 Component/KeyPath，QUIC UDP `4433` 使用原 Component，确保 MSI 升级不会跳过新增规则；注册测试等待真实 Announce |
| REQ-PICOO-DISCOVERY-002 | implemented | ARCH-PICOO-DISCOVERY-001 | 广播仅含白名单字段；`platform` 使用 `windows` / `macos` 标准值，fingerprint_prefix 来自公钥 | TXT 白名单 + 平台枚举往返/非法值拒绝 + 指纹前缀测试 |
| REQ-PICOO-DISCOVERY-005 | implemented | PUC-002 | Android 使用系统 NSD 浏览 Receiver，但 TXT 协议字段由 Rust Core 统一校验；API 33+ 发现明确绑定 Wi-Fi NetworkRequest，不随默认 VPN 路由漂移；系统浏览异常停止后自动恢复，网络切换或 Receiver 刷新 TXT 导致的瞬时 ServiceLost 不立即删除设备 | `NsdReceiverBrowser` 将原始 TXT 交给 JNI `parseDiscoveryTxt`；平台 TXT Map 仅以 iterator 单次复制，不调用部分 ArrayMap/HyperOS 不支持的 `EntrySet.toArray()`；`DiscoveryTxtTest` 覆盖 iterator-only Map；Rust `ReceiverAdvertisement::from_txt_properties` 强制完整字段、白名单、platform 与 pairing_state；Wi-Fi `NetworkRequest`；NSD 失败延迟重启、ServiceLost 设 10s 宽限且重新解析后取消；Devices 行展示真实 Windows/macOS 平台及 Ready/Paired |
| REQ-PICOO-DISCOVERY-006 | implemented | PUC-002 / PRD §8.1 | 健康网络发现 P50&lt;2s；已配对连接建立 &lt;3s | `paired_connect_to_streaming_under_three_seconds`；`resolved_service_event_updates_and_removes_cache` 验证真实事件映射与移除；`browser_discovers_local_advertiser_under_two_seconds` 仅在可控 LAN 多轮采样，不以内存 cache 更新时间冒充网络发现 P50 |
| REQ-PICOO-DISCOVERY-007 | implemented | PUC-008 / PRD FR-DISC-003 | mDNS 不可用时，Sender 可输入局域网 `IP:端口` 直连 Receiver；最近一次有效输入作为 Endpoint 定位信息保存在本机，但不建立信任；Android 的局域网 QUIC 优先显式绑定物理 Wi-Fi，不随默认 VPN 路由漂移；非 bypassable 分流 VPN 拒绝绑定但明确排除当前 Wi-Fi 直连目标时仍可连接，VPN 接管或禁止局域网时应提示用户允许局域网访问或关闭 VPN | Android `DevicesScreen` 在连接失败、弹窗重开与进程重启后保留最近 Endpoint；`SenderNativeRuntime` 用生命周期化 `NetworkCallback` 保存当前物理 Wi-Fi，并由 `configureQuicWifiNetwork` 将 `Network` handle 及严格的系统 LAN route fallback 门禁交给 Rust；Wi-Fi 缺失、VPN 阻止与同步失败状态收尾可区分；Rust Sender 缓存连接身份并在异步 QUIC 建连后发送 ClientHello；桌面等待页显示连接地址；`client_hello_queued_before_async_connect_is_sent_when_connected`；`manual_endpoint_connects_to_streaming` |
| REQ-PICOO-DISCOVERY-008 | implemented | PUC-002 / ARCH-PICOO-DISCOVERY-001 | iOS 在用户发起连接流程时请求 Local Network 权限并发现 Receiver；发现和 QUIC 连接均绑定 Network.framework 选出的物理 Wi-Fi 接口，不随普通 VPN 默认路由漂移 | `PicooWifiNetworkMonitor` 提供 Wi-Fi interface name/index；`PicooDiscoveryBrowser` 将 Rust mDNS browser 限定到该接口；Quinn socket 使用 `IP_BOUND_IF` / `IPV6_BOUND_IF`；`NSLocalNetworkUsageDescription` + `_picoocam._udp` Bonjour 声明；发现不可用/拒绝权限时保留经校验的 IPv4 或方括号 IPv6 `IP:端口` 直连；真局域网/VPN 组合仍待真机验证 |
