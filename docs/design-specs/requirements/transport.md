# REQ-PICOO-TRANSPORT：QUIC 传输

| ID | 状态 | 来源 | 描述 | 验收 |
| --- | --- | --- | --- | --- |
| REQ-PICOO-TRANSPORT-001 | implemented | ARCH-PICOO-TRANSPORT-001 | 业务层只依赖 `PicooTransport` trait | Quinn/Rustls 仅在 `picoo-transport` |
| REQ-PICOO-TRANSPORT-002 | implemented | ARCH-PICOO-TRANSPORT-001 | 控制消息走可靠 Stream，视频走 Datagram | loopback / testkit 验证 |
| REQ-PICOO-TRANSPORT-003 | implemented | PUC-002 | Receiver 为 QUIC Server，Sender 为 Client | QuicReceiver/QuicSender 角色测试 |
| REQ-PICOO-TRANSPORT-004 | implemented | PUC-006 | 重连退避 500ms→1s→2s→5s→5s | `ReconnectBackoff` + `reconnect_backoff_escalates_across_failed_attempts` |
| REQ-PICOO-TRANSPORT-005 | implemented | PRD §22 验证 A / ARCH-PICOO-TRANSPORT-001 | Android/Windows Quinn + Rustls 可构建链接，QUIC 构建不依赖 CMake/BoringSSL/NASM | Android：`cargo ndk`→Rust JNI `.so` + Gradle APK/AAB（含 16KB 门禁）；Windows：`xtask build/package windows` 产出 exe/DLL/MSI（CI `windows-latest`） |
| REQ-PICOO-TRANSPORT-006 | implemented | PRD §22 验证 A / ARCH-PICOO-TRANSPORT-001 | Quinn + Rustls Core 可为 ARM64 Apple device/simulator 与 macOS Receiver 构建链接，且不引入 CMake/BoringSSL/NASM | `xtask build ios` 链接 `aarch64-apple-ios`、`aarch64-apple-ios-sim`；`xtask build macos` 链接 ARM64 GPUI Receiver；Apple CI artifact |
| REQ-PICOO-TRANSPORT-007 | implemented | ARCH-PICOO-SESSION-001 | 一个 H.264 Access Unit 的全部 Datagram 必须作为一个有界队列项提交；拥塞时丢弃完整非关键 AU，不允许因应用队列或 Quinn send buffer 淘汰单个分片而主动制造半帧；关键帧可淘汰陈旧 delta，但自身必须完整容纳 | `PicooTransport::send_video_batch`；Quinn video queue 以 3 个 AU 为上限；发送前以 `datagram_send_buffer_space` 做整 AU 判定；Sender `flush_pending` 单次 batch 提交 |
| REQ-PICOO-TRANSPORT-008 | implemented | ARCH-PICOO-RUNTIME-001 | Receiver 上层只能关闭 Transport 当前活动会话，不得构造或缓存可能过期的 SessionId | `QuicReceiverTransport::close_active`；第二个 connection generation 主动关闭回归测试 |
| REQ-PICOO-TRANSPORT-009 | implemented | ARCH-PICOO-RUNTIME-001 | 平台 Session 使用事件/wakeup 驱动正常控制与恢复，250/500ms polling 仅承担 timeout 和 stats | QUIC actor 每次成功入队事件后推进 `TransportEventWake` revision；事件先到、并发 pump 已消费、超时和真实 QUIC control 均有回归测试；Android `Dispatchers.IO` 与 iOS detached task 只在后台阻塞并在唤醒后回到 UI actor；Android/iOS 构建与 iOS Swift 6 测试；真机空闲功耗仅作为性能基准证据 |
| REQ-PICOO-TRANSPORT-010 | implemented | ARCH-PICOO-TRANSPORT-001 / PUC-002 / PUC-006 | Android/iOS Sender 的每个 Quinn UDP socket（含自动重连）必须优先显式绑定平台选出的物理 Wi-Fi，不使用进程级默认网络切换；Android 非 bypassable 分流 VPN 拒绝绑定时，仅当数字目标位于当前 Wi-Fi 直连前缀且 VPN 路由不匹配该目标，才允许使用系统已验证的局域网分流；强制策略接管或禁止局域网时 fail closed | `ClientNetworkBinding`；Android `Network.getNetworkHandle()` → NDK `android_setsocknetwork`，失败后的 fallback 由 Wi-Fi `LinkAddress` 与默认 VPN `LinkProperties.routes` 双重门禁；iOS Network.framework interface index → `IP_BOUND_IF` / `IPV6_BOUND_IF`；同步连接失败恢复 `Disconnected`；`synchronous_connect_failure_returns_to_disconnected`、前缀单测、零值拒绝与 macOS socket 绑定单测；Android NDK、iOS device/simulator 构建 |
| REQ-PICOO-TRANSPORT-011 | implemented | ARCH-PICOO-RUNTIME-001 / ARCH-PICOO-SESSION-001 | Quinn 读取每批首个视频 Datagram 时记录 Receiver 本地单调时间并随批次传到 Reassembly；入口队列等待计入同一媒体 deadline，超过 deadline 的批次按完整 AU 建立 terminal boundary 并触发既有参考链恢复，容量上限不得冒充延迟上限 | `ReceivedVideoPacketBatch.received_at`；`receive_queue_age_ms` 与 `receive_queue_expired_access_units` 分层诊断；过期 AU 不增加网络丢片统计、迟到尾片不能重建且关键参考帧触发 IDR 的 packet/receiver 测试 |
