# REQ-PICOO-MEDIA：跨平台媒体管线

| ID | 状态 | 来源 | 描述 | 验收 |
| --- | --- | --- | --- | --- |
| REQ-PICOO-MEDIA-001 | implemented | PUC-005 | Android Camera2 + OES/EGL 平台合成器 + MediaCodec InputSurface 硬件 H.264；本机 TextureView 按实际相机缓冲尺寸与传感器/显示方向等比 contain，不允许拉伸或先行裁掉电脑端构图范围；OEM Camera2/EGL/Surface 同步异常必须收敛为可见编码错误，不得杀死应用进程 | `Camera2MediaEncoder` 以 Camera/Codec generation 隔离过期回调，在 Camera2 callback thread 枚举并打开 Camera、按 SurfaceTexture identity 重建会话；`CameraEncodingCompositor` 在专用线程持有 EGL/OES 并随 codec generation 释放；`outputSurfaceLock` 串行化 preview/compositor/codec Surface 的配置与释放；`CameraPreviewSurface.calculatePreviewTransform`、`CameraEncodingCompositorTest`；码率配置来自 Rust Core（真机预览/编码仍待） |
| REQ-PICOO-MEDIA-002 | implemented | PUC-005 | 480p30 / 720p30 / 1080p30 能力协商与回退；中途分辨率切换 | Rust 以 `receiver_max_height` 约束 preferred height 与 ABR 目标，平台对遗留超能力指令报告未启动失败、实际重建编码器并持续上报 generation 事实；`receiver_capability_caps_preferred_height_in_rust`；JVM OEM 回退 |
| REQ-PICOO-MEDIA-003 | implemented | PUC-005 | Rust 为前后摄切换、分辨率变化、编码器重建与连接恢复分配候选 stream_epoch；只有 transaction/generation/epoch/height 全匹配且已 stage StreamConfig 的首个 IDR 才提交，失败/超时/断连不复用 epoch | `EncoderApplyState` + `EncoderStarted/NativeEncoderAccessUnit/EncoderFailed`；Rust 持有 3s deadline、rollback snapshot 与 recovery/disconnect 决策；提交 IDR 同时进入 packetization；Android/iOS 只执行 directive；epoch 在 Android `Int32.max` fail-fast；事务、迟到事实、恢复失败与跨通道 StreamConfig/IDR 竞态回归 |
| REQ-PICOO-MEDIA-004 | implemented | PUC-005 | 本机预览镜像与远端输出镜像独立 | Android `LocalPreviewMirror` 按前后摄默认；Receiver `transform_nv12` 在 upright output space 应用 `StreamConfig.mirrored` |
| REQ-PICOO-MEDIA-005 | implemented | ARCH-PICOO-MEDIA-001 / ARCH-PICOO-RUNTIME-001 | Windows Receiver 正式桌面二进制必须通过 Cargo feature 边把 `windows-mf` 传递到媒体解码 crate，并使用 MF H.264 Decoder 输出 NV12；实时链路必须启用 MF low-latency，MFT 输入类型必须携带已知的视频形状，输出 Sample 必须遵循 `GetOutputStreamInfo` 的分配模型；锁定 MF buffer 后直接生成最终紧凑 NV12，紧凑布局只复制一次，row pitch 或宏块对齐 allocation height 则直接复制可见平面，不能先复制完整 padded allocation；所有错误路径 RAII Unlock；生产解码失败不得以 stub 假帧伪装为实时画面；D3D11-aware 与 device-manager 接入状态必须分别诊断，不能仅凭 MF MFT 冒充硬件加速；Linux/CI 使用 OpenH264 软解 | `picoo-desktop/windows-vcam` → `picoo-receiver/windows-mf` → `picoo-media-decode/windows-mf` feature 图单测；紧凑输入规范化保持 Vec pointer identity；`normalizes_macroblock_aligned_1088_allocation_to_visible_1080` 与 row-pitch 回归；Windows `paired_avcc_length_prefixed_au_reaches_latest_frame_store`；MF 错误向 Receiver/桌面状态透传；能力日志区分 `d3d11_aware`、`d3d_manager_attached=false` 与 `output_storage=cpu-nv12`；D3D11 零拷贝与 Windows 真机画面仍待后续验证 |
| REQ-PICOO-MEDIA-006 | implemented | ARCH-PICOO-MEDIA-001 | Receiver 单次解码、LatestFrameStore 多路消费 | `decode_invocations == access_units` 测试 |
| REQ-PICOO-MEDIA-007 | implemented | PUC-006 | 动态码率阶梯由 Rust 唯一维护；Android/iOS 只应用 Rust 返回的目标码率 | rate-control + FFI `picoo_bitrate_*_for_height` + Android MediaCodec / iOS VideoToolbox |
| REQ-PICOO-MEDIA-008 | implemented | PUC-005 | 手机端曝光补偿可调 | `Camera2MediaEncoder.setExposureCompensation` + Streaming EV±（真机验证仍待） |
| REQ-PICOO-MEDIA-009 | implemented | PUC-005 | StreamConfig.rotation 驱动 LatestFrameStore/VCam 朝向 | `transform_nv12` 在 publish 前直立像素；metadata 清零；dims 随 90/270 交换 |
| REQ-PICOO-MEDIA-010 | implemented | PUC-006 | 持续拥塞 1080→720→480；健康后可回升；过热强制 ≤720p；ABR 分辨率变化必须由 Rust 在匹配原生事实后提交 | rate-control `EncoderDirective` + FFI/JNI directive/fact API；失败重建由 Rust 选择 rollback/recovery/disconnect；`thermal_hold` |
| REQ-PICOO-MEDIA-011 | implemented | ARCH-PICOO-MEDIA-001 | iOS 使用 AVFoundation 采集并由 VideoToolbox 低延迟硬件编码 H.264；原始帧不跨 Rust FFI | `AVCaptureVideoDataOutput` 原生 420v → `VTPixelTransferSession` 854×480 缩放（按需）→ 必需硬件 `VTCompressionSession`（Realtime / 无 B 帧 / 2s IDR）→ AVCC AU + SPS/PPS → `PicooCore`；AU handoff 有界且连接/epoch 切换等待新 IDR，Rust 断连清空待发分片；Swift 6 App 与测试 bundle 已编译链接，测试执行待 CI；480p/720p/1080p30、方向、切换 3s 恢复和弱网 ABR 仍待 iPhone 真机升级为 `verified` |
| REQ-PICOO-MEDIA-012 | implemented | ARCH-PICOO-MEDIA-001 | macOS Receiver 使用 VideoToolbox 单次硬件解码至 NV12 LatestFrameStore | `objc2` Apple Framework 绑定将 Annex-B/AVCC + SPS/PPS 封装为 CoreMedia Sample，要求硬件 `VTDecompressionSession`，同步解码并按 plane stride 复制紧凑 `420v` NV12；静态真实 H.264 IDR 覆盖 Annex-B、AVCC、无假帧错误、SPS/PPS 重建，以及 Receiver 720p→480p ABR/epoch/LatestFrameStore 恢复；断连、StopStream、关闭均 flush session；`xtask test macos` 的产品和测试树均不需要 OpenH264/CMake。Android/iOS→GPUI/Camera Extension 真机链路仍待升级为 `verified` |
| REQ-PICOO-MEDIA-013 | implemented | PUC-005 / ARCH-PICOO-MEDIA-001 | Android 无论横持或竖持，编码输出都保持直立的横向 16:9；横持尽量利用完整画面，竖持采用中央 cover 裁切；竖持 1080p 优先选择短边至少 1920 且能达到目标帧率的最小 Camera2 源，避免把 1080 像素窄带上采样；Sender 本机取景器全屏 center-cover、无黑边且不叠加小型构图参考框，不提供 9:16 输出模式；Receiver 不承担 Android 构图修正 | Android `CameraEncodingCompositor` 在 Camera2 OES 纹理与 MediaCodec Input Surface 之间执行 GPU 旋转/中央裁切，`EncodingTransform` 先合并平台 `SurfaceTexture` producer matrix 与目标方向，再按实际纹理轴的像素跨度计算等比 16:9 cover，避免 OEM 已交换纹理轴时对 4:3 源重复换轴并拉伸；稳定矩阵跨帧缓存，`StreamConfig.rotation = 0`；`CaptureSizeSelector.portraitCrop` 与 Camera2 min-frame-duration 过滤；`CameraPreviewSurface` center-cover + 全屏 `StreamingScreen`；`EncodingTransformTest` 覆盖小米真机 producer axis-swap 下 4:3 与方形源，另有 `CameraEncodingCompositorTest`、`CaptureSizeSelectorTest`、`connectedPreviewIsFullScreenWithoutAReferenceFrame`；小米真机至 macOS 已验证 480p/720p/1080p 解码尺寸、480p 5 秒 30.0 FPS 帧序列，并以同场景物体对照和用户目视确认 480p/720p 比例正常 |
| REQ-PICOO-MEDIA-014 | implemented | ARCH-PICOO-MEDIA-001 | V1 所有分辨率的 SDR H.264 颜色在 Sender、Receiver 预览与 Windows 虚拟摄像头 media type 中统一为 BT.709 limited range；编码实时模式不产生 B 帧，优先 CBR 以限制排队和瞬时码率 | LatestFrameStore NV12→RGBA 与 Windows MF/VCam media type 使用 BT.709 limited；Android MediaFormat 使用 `COLOR_STANDARD_BT709` / `COLOR_RANGE_LIMITED` / `COLOR_TRANSFER_SDR_VIDEO`、`KEY_MAX_B_FRAMES=0` 与受能力约束的 CBR；`preview_uses_bt709_limited_range` |
| REQ-PICOO-MEDIA-015 | implemented | ARCH-PICOO-SESSION-001 / PUC-006 | Receiver 通过可靠控制流发出的自动 IDR 请求必须由 Android Sender 在下一次编码帧回调时消费并立即请求 MediaCodec sync frame；即使当前 AU 因有界队列压力未入队，也不得退化为只等待 500 ms 维护轮询 | `EncodedFrameListener` 帧级消费与维护轮询兜底；Xiaomi 15 → macOS 真机链路在不可恢复 AU 于 200.3 ms 到期后约 132 ms 收到完整 IDR，随后恢复 30 FPS，Sender AU queue drop 为 0 |
| REQ-PICOO-MEDIA-016 | implemented | ARCH-PICOO-RUNTIME-001 | Rust Core 独占编码器 apply/commit/rollback/recovery/timeout 事务语义；Android/iOS 只执行 directive 并报告带 transaction/generation/epoch/height 的事实 | `EncoderApplyState` 统一 local/ABR/recovery transaction、3s deadline、staged config 与 rollback snapshot；C/JNI/Swift/Kotlin 旧 ACK/NACK/cancel API 已删除；匹配首个 IDR commit、迟到 callback 隔离、启动前 rollback、启动后 recovery、恢复失败 disconnect 及平台构建测试 |
| REQ-PICOO-MEDIA-017 | verified | ARCH-PICOO-RUNTIME-001 | EncodedAccessUnit/VideoFrame 贯通 generation、frame_id、source PTS、Receiver arrival/decode 时间并转移 buffer 所有权；无方向变换时 Decoder→LatestFrameStore 零复制，需要变换时 rotate+mirror 合并为单次最终输出写入，拒绝跨 generation 旧帧；owner 路径记录方向变换 count/total/max CPU 时间，只有 profile 证明其为热点才改变线程所有权 | LatestFrameStore/`transform_nv12` 原指针复用测试；融合方向与 padded stride 回归；跨 generation 门禁；每 AU 最多解码一次；诊断导出 `orientation_transform_*`；`nv12_transform` release benchmark |
| REQ-PICOO-MEDIA-018 | implemented | ARCH-PICOO-RUNTIME-001 | Decoder Worker 与 Session Pump 解耦；最多两个 pending AU，新 IDR 取代排队媒体，reference delta 优先替换 discardable delta；未过硬期限的 reference AU 在队列暂满时留在 Jitter 等待 capacity event，超期才进入 IDR 恢复，饱和时 discardable AU 可直接丢弃；Reset/close/drop 不等待正在执行的像素处理，完成结果受 decoder/connection/stream generation 三重门禁 | `queue_is_bounded_and_drops_discardable_before_reference_media`、Decoder admission 矩阵；`ready_reference_waits_in_jitter_until_decoder_capacity_is_available`；诊断导出 `decoder_capacity_dropped_access_units`；`reset_invalidates_an_active_decode_generation`；`slow_decoder_never_blocks_session_close/drop`；平台 Decoder、恢复、重连全套测试 |
| REQ-PICOO-MEDIA-019 | implemented | ARCH-PICOO-RUNTIME-001 | 删除 macOS Preview NV12→BGRA→NV12 双转换；受当前 GPUI 仅接受 `420f` 且 shader 固定 BT.601 full 的契约限制，后台 worker 在 NV12 域用可复用的 `fast_image_resize` Resizer 与 scratch 缩放 Y/UV 双平面并执行一次 BT.709 limited→BT.601 full 变换，最终只复制一次到有界 IOSurface pool；紧凑且需要缩小的解码帧直接借用为缩放输入，不先生成完整 NV12 副本 | 黑/白端点、红色色彩、偶数 NV12 resize、stride/plane size、三槽 pool recycle 与资源复用单测；macOS desktop 全目标编译；生产 macOS 路径无 BGRA 中间帧，真机帧率/profile 仍待升级 verified |
| REQ-PICOO-MEDIA-020 | implemented | ARCH-PICOO-RUNTIME-001 | Sender 在同步分包期间直接借用原生 AU，复用 fragment descriptor、FEC data/parity shard scratch 与发送调度容器并生成自持有的最终 Datagram，不为异步生命周期额外复制整帧；Transport 只接收完整 AU batch且 Quinn Worker 不再二次编码，入站 Quinn Bytes 零复制解包；FEC 按损伤与帧重要性自适应 0/1/2 parity，Light 只计算一个 parity | `adaptive_fec_emits_zero_one_or_two_parity_shards`；Light parity 0 与 Strong parity 0 wire 字节一致；`fec_tracks_frame_importance_and_receiver_loss`；借用/owned AU 分包等价、有界 AU 队列、无拒绝帧号缺口、batch/MTU 门禁与 QUIC roundtrip；健康 delta 无固定 33% parity，IDR 保持 2 parity；release benchmark/真机 CPU 与带宽 profile 仍待升级 verified |
| REQ-PICOO-MEDIA-021 | implemented | ARCH-PICOO-RUNTIME-001 | Android MediaCodec output callback 只复制有效 AU、释放 codec buffer 并提交有界 GOP-aware handoff；单一 Sender media worker 执行 generation/transaction 校验、可选 StreamConfig、分包、flush、pump 与 IDR 消费，一帧只通过一次原子 JNI 并只取得一次 Session mutex；不得让 MediaCodec 借用指针越过 `releaseOutputBuffer`；JNI 负错误码必须先进入 typed failure，禁止作为成功副作用位标志解释 | `EncodedAccessUnitBuffer` 同时限制 12 AU、16 MiB 与 250 ms 帧龄，溢出或超龄后等待 keyframe；`EncoderSubmitOutcomeTest` 覆盖 -1/-2/0/合法组合，失败不记录 accepted、不请求 IDR 且恢复 config dirty；JVM buffer/generation/关闭测试、Android NDK 构建；codec callback p95/p99 与真机 CPU/GC 仍待 profile |
| REQ-PICOO-MEDIA-022 | implemented | ARCH-PICOO-RUNTIME-001 | Android JNI 与 iOS C ABI 为每个原生编码 AU 提供一个完整事件入口；Core 在一次 Session 独占中统一处理 transaction/generation、可选参数集、StreamConfig 控制先行、packetization、flush/pump 与 IDR 请求，FFI 不编排业务调用顺序；平台必须分别消费 `encoder_accepted`、`stream_configured` 与 `keyframe_requested`，FFI 调用成功不得冒充 Core 已接纳编码事实 | `complete_native_encoder_event_is_core_ordered_and_directly_testable` 验证第一个视频 batch 前已有可靠配置；Android typed bit flags；iOS `rejectedEncoderOutcomeRemainsRejected` 与类型化 `EncoderSubmitResult`；平台编译由 CI 验证 |
| REQ-PICOO-MEDIA-023 | implemented | ARCH-PICOO-RUNTIME-001 | Decoder 输出将不可变帧描述与 backing storage 分离；当前只启用 `CpuNv12(Bytes)`，帧契约显式携带格式、BT.709 limited range、stride、旋转和时间戳；未来原生 surface 必须具有安全可转移 owner，不得给裸平台指针添加宽泛 `Send` | 全平台 decoder 构造/Receiver 消费迁移；`picoo-media-decode` 回归；CPU NV12 accessor/ownership 测试；原生 surface 仍属后续 profile 驱动范围 |
