# REQ-PICOO-STACK：Monorepo 与构建

| ID | 状态 | 来源 | 描述 | 验收 |
| --- | --- | --- | --- | --- |
| REQ-PICOO-STACK-001 | implemented | ARCH-PICOO-STACK-001 | Cargo workspace 包含 ARCH 定义的全部 Rust Core crate | workspace members + `cargo test` |
| REQ-PICOO-STACK-002 | implemented | ARCH-PICOO-STACK-001 | `proto/picoo_camera.proto` 由 prost 生成控制消息类型，构建期使用 vendored `protoc` | `picoo-protocol` build.rs；开发机无需单独安装 `protoc` |
| REQ-PICOO-STACK-003 | verified | ARCH-PICOO-STACK-001 | Android 通过 Rust JNI exports、iOS 通过 XCFramework Clang module 的 C ABI 调用 Rust Core | Android 以 `apps/android/toolchain.properties` 固定当前 AGP 8.5.2 已验证组合 `cargo-ndk 4.1.2` + NDK `28.2.13676358`（r28c），Gradle、本机与 CI 共用构建前校验，单一 Rust JNI `.so` 通过 16KB `PT_LOAD` 门禁；NDK 与 AGP/Gradle 统一升级，不单独漂移；iOS `PicooCore.xcframework` 携带 header/module map，`PicooSenderSessionTests` 已在远端 Simulator 覆盖 Swift 创建、查询、销毁 Sender handle；CI run `33276518983` |
| REQ-PICOO-STACK-004 | implemented | ci-and-build.md | xtask 提供 build/test/package 统一入口，并由平台测试入口验证最终产品使用的 Cargo feature 图和关键原生实现，禁止用一次彼此独立的依赖 crate 构建冒充 feature 已进入产品二进制 | `cargo xtask --help`；`test protocol` / `test linux` / `test ios`；Windows test 显式启用 `picoo-media-decode/windows-mf` 与 `picoo-receiver/windows-mf` 的 AVCC→MF→LatestFrameStore 测试；桌面 feature 边单测；`package android/windows/macos` |
| REQ-PICOO-STACK-005 | implemented | ci-and-build.md | GitHub Actions ubuntu + windows job 产出隔离身份的 Debug APK 与 Windows 安装包；用户版本来自 workspace SemVer，CI 以单调递增的 `github.run_number` 作为统一 `PICOO_BUILD_NUMBER`；正式 Android APK/AAB 只由受保护 release workflow 生成；Android 映射到 `versionCode`，Windows 映射为 Installer 实际比较的第三字段 | 普通 CI `assembleDebug` + 16KB/app ID 门禁；受保护 `xtask package android`→签名 APK/AAB；NDK 28；Gradle 从 workspace 读取 `versionName`；Windows job 钉扎 WiX 5.0.2 + `PICOO_REQUIRE_MSI=1`；MSI Property/sequence/ICE 与 artifact hard-fail |
| REQ-PICOO-STACK-006 | implemented | ARCH-PICOO-STACK-001 | 仓库构建不依赖 CMake 或 C++/MSBuild 工程；QUIC、Android JNI 与 Windows VCam 均由 Cargo 管理 | 仓库无 `CMakeLists.txt`、VCXPROJ、C++ VCam Source 或对应构建调用；Windows VCam 为 `windows-rs` `cdylib`；CI 不安装或调用 CMake/MSBuild |
| REQ-PICOO-STACK-007 | implemented | ARCH-PICOO-STACK-001 / ci-and-build.md | Apple 平台具有独立、无签名的 ARM64 可编译与 bundle 基线；iOS/macOS 用户版本来自 workspace SemVer，普通 CI 的 `CFBundleVersion` 使用统一递增构建号，macOS Host 与 Camera Extension 必须完全一致 | 已验证 ARM64 GPUI Receiver、device/simulator XCFramework、SwiftUI App 完整链接与 Simulator XCTest；`xcodebuild` 显式接收 `MARKETING_VERSION` / `CURRENT_PROJECT_VERSION`；嵌入 Camera Extension 的 macOS Host `.app` 已进入本地打包门禁，待当前变更远端 CI 复核后恢复 `verified`；签名和真机不在本 REQ 验收范围 |
| REQ-PICOO-STACK-008 | implemented | ARCH-PICOO-STACK-001 / ARCH-PICOO-DISCOVERY-001 | Release 产物具有稳定发行身份：Android signer 缺失 hard fail、Debug 使用独立 applicationIdSuffix；Windows/macOS/iOS 建立稳定签名/公证边界；CI 核对 signer fingerprint、application ID、version/build，并附 SBOM 与 provenance | Android：固定 signer 的 APK/AAB；Windows：先签三个 PE、再用已签 PE 重建并签 MSI，强制 RFC3161 timestamp、Code Signing EKU、有效期与固定 SHA-256；macOS：Developer ID、Hardened Runtime、授权 profile、公证与 staple；iOS：Apple Distribution、App Store profile、Team/Bundle/version/arm64/effective-entitlements/embedded-profile 门禁。四端受保护 workflow 均在第三方 artifact 处理前清除凭据，并生成 Syft SPDX SBOM 与 GitHub provenance；首次真实保护凭据绿测及覆盖升级仍是外部验证门槛 |
| REQ-PICOO-STACK-009 | implemented | ARCH-PICOO-RUNTIME-001 | 建立虚拟时钟 `picoo-sim` 与夜间网络损伤/soak，覆盖 Camera→Encoder→Sender→Transport→Receiver→Decoder→FrameSink 完整状态机；快速模式服务大范围状态回归，生产等价模式启用自适应 Jitter 与具有 active/pending 容量、虚拟完成时刻和 reset 的异步 Decoder | `picoo-sim` 覆盖认证门禁、FEC 丢包/乱序/重复/burst、IDR fragment loss、晚到 StreamConfig、连续 encoder transaction/失败、前后台、快速重连、Receiver 重启、控制回放/越序/非法阶段、慢 Preview/快 VCam、固定 seed 与有界队列；`production_timing_*` 覆盖 Decoder 暂时饱和与真正硬过期；nightly 运行四 target fuzz 与 30 分钟 paired loopback soak；通过定时执行记录后再升为 verified |
| REQ-PICOO-STACK-010 | implemented | ARCH-PICOO-RUNTIME-001 / ARCH-PICOO-FRAME-001 | Shared Ring 原始布局、共享 buffer 所有权与可解释执行的 C ABI 进入 strict-provenance Miri；ready/sequence/reader lease 使用 Loom 穷举关键并发交错；OS mmap、进程锁与平台 API 继续由平台 contract/stress 验证 | 奇数 slot capacity 的对齐/边界 Miri 回归；FrameBufferPool 最后引用归还；C string 与 SPS/PPS raw pointer FFI；`shared_ring_ready_and_reader_lease_protocol`；nightly `cargo xtask test miri/loom` |
| REQ-PICOO-STACK-011 | implemented | ARCH-PICOO-RUNTIME-001 | 生产 Receiver 与 `picoo-sim` 直接复用同一个纯媒体调度决策，模拟器不得复制旧 AU 阻塞、播放点、Decoder 准入等待或硬过期算法；Reassembly 完成 AU 直接进入 Jitter，不得另建 completed-AU 等待队列 | `picoo-sim` 依赖 `picoo-receiver` 并调用 `schedule_media`；生产等价自适应 Jitter/异步 Decoder pipeline 与 Receiver scheduler 测试 |
