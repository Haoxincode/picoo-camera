# REQ-PICOO-VCAM：桌面虚拟摄像头

Next v2 中的格式与后端契约由 `REQ-PICOO-NEXT-013/014/030/036` 和
`ARCH-PICOO-MEDIA-002` 覆盖；本表中的旧 480p/单一 CPU 输出描述只保留为历史追溯，不能
作为当前产品验收标准。

### Windows GPU sample 有效长度（REQ-PICOO-VCAM-012 / REQ-PICOO-NEXT-014）

`GpuNative` 的 NV12 DXGI buffer 在加入 sample 前必须设置有效数据长度；
仅持有纹理不代表 `IMFMediaBuffer` 已声明可消费的数据。沿用系统
`MFCreateDXGISurfaceBuffer`，通过 `GetMaxLength` / `SetCurrentLength` 完成元数据设置，
不增加像素复制、第三方依赖或后端切换。
依据为 [Microsoft IMFMediaBuffer 契约](https://learn.microsoft.com/en-us/windows/win32/api/mfobjects/nf-mfobjects-imfmediabuffer-setcurrentlength)
及 [Chromium DXGI sample 实现](https://chromium.googlesource.com/chromium/src/+/4ad0f4d64a1064f2528aa372f85a9ab9c7050709/media/gpu/windows/media_foundation_video_encode_accelerator_win.cc)。
Windows 回归测试通过生产 `make_native_sample` 检查 720p/1080p 的 buffer/sample 有效长度、
时间戳和原始 DXGI 纹理身份；它不替代真实 Frame Server 和飞书会议验收。

2026-09-22 用户反馈飞书会议纯绿屏、Windows 桌面预览正常。当前源码缺少上述有效长度设置，
已补齐；本机为 macOS，Windows 原生测试与飞书复测仍待，不能将该遗漏直接认定为已确认根因。

2026-09-23：CI #642（`a81358bb`）全部通过，包括新增 Windows sample 测试；用户复测
1.642 仍为飞书绿屏。因此有效长度修补不是本次问题的充分修复，会议软件验收仍未通过。
后续检查发现 CpuBridge 读回共享 NV12 目标时未获取 keyed mutex，补齐访问 lease 并增加
共享纹理忙碌拒绝与已知 Y/UV 像素回归。实际采用的后端、失败阶段仍需现场日志确定。

| ID | 状态 | 来源 | 描述 | 验收 |
| --- | --- | --- | --- | --- |
| REQ-PICOO-VCAM-001 | implemented | PUC-004 | 注册统一 base name `Picoo Camera`；允许 Windows 追加/本地化系统 Virtual Camera 后缀 | `FRIENDLY_NAME` / `PICOO_VCAM_FRIENDLY_NAME`；CI `verify_windows_bundle.ps1` UTF-16 嵌入校验；桌面状态通过 `MFEnumDeviceSources` 精确匹配受保护注册表中持久化的 symbolic link，不再以 DLL/COM、显示名精确相等或任意同名设备冒充 Active；独立 Win11 Host Contract 从安装目录执行同一 exact-link 枚举并激活 Source，首次 self-hosted 绿测前不冒充 `verified` |
| REQ-PICOO-VCAM-002 | implemented | ARCH-PICOO-VCAM-001 | MFCreateVirtualCamera + 独立 Rust IMFMediaSource DLL | per-machine MSI/显式 UAC 修复创建 AllUsers + System-lifetime 设备，普通桌面启动不重复创建 Session 设备；`windows-rs` 实现 COM/MF，Source 满足 `IMFMediaSourceEx` / `IMFMediaStream2` / `IMFGetService` / `IKsControl` 契约；MF 标准接口使用直接 COM 引用，禁止触发 `REGDB_E_IIDNOTREG` 的 Agile Reference 包装；Start 失败 best-effort Remove，卸载 Remove 不依赖 Source Start；Next v2 客户端协商 720p/1080p × 30/60 NV12 后输出类型在运行期保持稳定；Windows CI 执行 DLL class factory → Source 激活 → Start/Stop/Shutdown 合约测试并产出 DLL；真机会议软件验收仍待 |
| REQ-PICOO-VCAM-003 | implemented | PUC-004 | Windows VCam 进程只读 Shared Frame Ring，不持有 QUIC/解码器；Windows Source 从 Local Service / Session 0 可访问的 ProgramData 文件环取实时帧。macOS 扩展不读 ring，只消费授权 Host 的 CMIO sink sample | Rust VCam Source 只依赖 `picoo-frame-hub` 与 Windows 系统 API；默认生产路径精确映射到 `%ProgramData%\Picoo Camera`，直接复用共享环与占位帧实现；ring-reader 使用相同路径诊断；macOS sink/source 合同见 `REQ-PICOO-VCAM-017` |
| REQ-PICOO-VCAM-004 | implemented | PUC-001 | 安装器注册 COM/MF 组件与卸载清理；系统级修复只由安装器或用户显式操作触发 | WiX `-arch x64` per-machine 包 + 声明式 64 位 COM 注册表 + FirewallException + ProgramData Shared Ring CreateFolder/继承 ACL（Local Service 与 Builtin Users 可读写）；安装/卸载 CA 使用 `Impersonate=no`，统一维护 AllUsers + System-lifetime 设备；注册和卸载均 `Return=check`，但注册 CA 只以 `IMFVirtualCamera::Start` 与 symbolic link 持久化作为事务成败，不得用 Windows Installer 服务会话中的异步 MF 枚举否定成功的 Start；repair 与 Start 失败不得删除操作前已存在的 identity；卸载直接 Remove，不以 Source Start 为前置，后续 identity 清理 best-effort，COM 交给 MSI 事务；`IMFVirtualCamera` 必须先 Shutdown/Release，再执行 `MFShutdown` 与 `CoUninitialize`，维护命令在 identity 不存在时幂等成功；注册持久化 symbolic link，交互用户上下文以有界重试观察 MF Software Device 发布；显式修复通过 `runas` 执行同一维护命令并复查大小写不敏感的精确 link，发布尚未收敛时显示 `Installed` 而非回滚或冒充 `Active`；Windows 所追加或本地化的显示名后缀不得造成 NotInstalled 误判；普通启动与检测不写 HKLM、不提权；专用 Win11 Host Contract 自动覆盖 fresh install、同版 repair、连续两次 remove、重新注册、卸载后 exact-link 消失与 Picoo-owned 文件/注册表清理，首次 self-hosted 绿测仍待 |
| REQ-PICOO-VCAM-005 | proposed | PUC-004 | Zoom/Teams/飞书会议/腾讯会议/OBS/浏览器可选用并取得实时帧 | [会议软件验收清单](../verification/vcam-meeting-apps.md)（需 Win11） |
| REQ-PICOO-VCAM-006 | implemented | ARCH-PICOO-VCAM-001 | macOS 使用 Core Media I/O Camera Extension 注册 `Picoo Camera` | Swift 6 CMIO source/sink 扩展已可编译；`package macos` 将 `com.haoxincode.picoo-camera.camera-extension.systemextension` 嵌入 Host `.app` 标准目录；Host 通过纯 Rust `objc2-system-extensions` 在后台查询、激活与停用，严格按 Bundled/待批准/重启后生效/移除中/Active 限定操作，并持久化跨重启 intent；仅更高 build 可替换；检测中与推流均不得冒充 Active；App Group 仅保留为发布签名/系统扩展 capability，不承载像素 IPC；仍需已签名真机上的用户批准、重启枚举、移除和会议软件验收；扩展不持有 QUIC/Decoder |
| REQ-PICOO-VCAM-007 | implemented | ARCH-PICOO-VCAM-001 / ci-and-build.md | macOS 发布产物满足 Hardened Runtime、Developer ID 签名与公证 | `cargo xtask release macos` 强制递增 release/build 版本并同步 Host/Extension；校验 Developer ID profile 的有效期、分发类型、Team/Bundle/App Group/System Extension capability 与授权证书，按内→外启用 Hardened Runtime/secure timestamp 签名，再复核实际 signer 和 effective entitlements，最后执行 `notarytool`、staple、`codesign --deep --strict` 与 `spctl`；release workflow 的 action 固定 SHA、凭据文件权限收紧并 always 清理；仍需凭据配置后的首次远端 release 绿测和签名真机验收 |
| REQ-PICOO-VCAM-008 | implemented | ARCH-PICOO-VCAM-001 | Windows MF Source 可观测每秒 RequestSample、fresh、cached、placeholder、failed 与 delivery time；每个 request 与 outcome 必须原子归入同一统计窗口；是否增加 pacing 必须由 Frame Server 真机指标决定，不能把外部项目的同步 sleep 直接作为平台规则 | Windows debug/诊断计数 + Teams/Zoom/OBS/浏览器真机记录；声明 30 FPS 时不得出现持续异常 request pump |
| REQ-PICOO-VCAM-009 | implemented | PUC-001 / ARCH-PICOO-VCAM-001 | Windows 安装、修复与卸载必须围绕当前产品身份形成单一事务；不定义跨产品契约的原位替换 | 当前 MSI 的注册、修复、幂等移除与完整卸载；CI 查询真实 MSI 表并执行 ICE27/ICE63/ICE77；产品契约变化时先卸载再安装当前配套版本，并重新验证 exact link、COM 路径与 MF 枚举 |
| REQ-PICOO-VCAM-010 | implemented | ARCH-PICOO-RUNTIME-001 | Windows RingReaderWorker 与 OutputPreparationWorker 只为 Running 状态下所有消费者实际协商的活跃格式后台生成 `Arc<PreparedFrame>`；Stop 后暂停 ring 读取与缩放；Cached Frame 只克隆 Arc，RequestSample 只做一次最终 MF buffer copy；需求 revision 变更与 cache invalidation、revision 校验与 cache publish 分别在同一状态锁内原子完成，旧结果或 placeholder 不得覆盖当前需求的新结果 | demand/cache 原子交错测试覆盖旧 revision 拒绝、当前结果发布和幂等激活；Ring view 直接构造 `Arc<[u8]>`，不经中间 Vec；Y/U8 与 UV/U8x2 使用可复用 `fast_image_resize` Catmull-Rom Resizer/scratch；单一 720p 消费者时 1080p 准备次数为零，Stop 后计数不增长；多消费者、格式切换、竖屏、letterbox、placeholder、指针复用、幂等 Shutdown 测试；`output_copy` release benchmark 验证缓存路径 0 allocation/frame、一次最终 copy；仍需 Frame Server 真机压测升级为 verified |
| REQ-PICOO-VCAM-011 | implemented | ARCH-PICOO-RUNTIME-001 | VCam 独立 SampleClock 保证 timestamp 单调、duration 固定；请求过快复用内容并推进节拍，请求过慢跳过旧时刻且不补积压 | 纯时钟测试覆盖首次锚定、快速请求、慢请求跳档、平台时钟回退与 Start/Stop 后重置；Windows Source 在最终 MF copy 前原子分配 timestamp；仍需 Frame Server + Teams/Zoom/OBS/浏览器 timestamp/duration 实测 |
| REQ-PICOO-VCAM-012 | implemented | ARCH-PICOO-RUNTIME-001 | 独立 VCam Contract Harness 不依赖手机或 QUIC，以合成 NV12 覆盖 Start/Stop/Start、并发 Request/幂等 Shutdown、allocator 替换、720p/1080p × 30/60 协商、Producer 暂停/崩溃/sequence 重置重启、双客户端和 host 重建 | 跨平台 `contract_harness` 固定检查 size/stride/buffer、generation 恢复与 timestamp；Windows in-process COM/MF contract 执行 allocator replacement、真实 `RequestSample`/`MEMediaSample` 的 NV12 buffer/固定 duration/时间戳检查，以及 Start/Stop/Start/Shutdown；真实 Frame Server/会议软件重建仍待主机验收升级为 verified |
| REQ-PICOO-VCAM-013 | deprecated | ARCH-PICOO-RUNTIME-001 | 历史 macOS App Group mmap Reader、扩展内 CPU 缩放与 ring 缓存契约 | 已由 `REQ-PICOO-VCAM-017` 的 CMIO sink/source 与 Host 侧统一 renderer 取代；`SharedRingReader`、C17 原子桥和 macOS mmap 实现已删除，不得作为当前验收路径 |

| REQ-PICOO-VCAM-014 | deprecated | ARCH-PICOO-MEDIA-002 / REQ-PICOO-NEXT-015、034 | 历史 Camera Extension Reader 侧三槽输出池契约 | 当前 live 图像由 Host 的 `GpuNative`/`CpuBridge` 固定三槽池拥有，扩展只缓存已消费 sink 图像并以独立 SampleClock 重复发送；现行容量与所有权由 `REQ-PICOO-VCAM-017` 验收 |

| REQ-PICOO-VCAM-015 | implemented | ARCH-PICOO-MEDIA-002 / REQ-PICOO-NEXT-014、028 | Mac VCam 提供 720p/1080p × 30/60 固定格式，默认 1080p60；独立有理数 SampleClock 按 host time 跳过过期槽，不随源回调变化，不积累整数周期舍入误差；格式事务保留时间单调性，后端操作不重置时间线 | 长期时钟、提前/回退/晚到 tick、30↔60、耗尽、属性非法值拒绝；Swift 生产代码 harness 与 Extension 编译；CMIO 实际客户端另验 |

| REQ-PICOO-VCAM-016 | implemented | ARCH-PICOO-MEDIA-002 / REQ-PICOO-NEXT-013、014、030、036 | Windows GpuNative 由 Frame Server 先声明已协商的固定 720p/1080p NV12 布局及 output revision，Receiver 再在源 D3D11 device 上创建同尺寸目标；源旋转、镜像和比例只在固定横向画布内处理，不得把竖屏源交换为竖向输出。Producer 按 Consumer 释放节奏复用最新不可变源帧，使独立 SampleClock 可在源帧率较低或短暂停顿时继续产生新时间戳，并按 source identity 区分 Fresh/Cached。格式或帧率事务推进 output revision 并重建 native session；旧会话的完成、错误或 keyed-mutex 释放不能清空新会话。Control channel 双向认证 Local Service Consumer 与同安装目录 Receiver；未认证 peer 不得提供或接管 HANDLE。Offer 成功发布即结束远程 HANDLE 回收权；Consumer 先拒绝零值、伪句柄和无效句柄，再以 RAII 在所有分支关闭一次目标 HANDLE，producer surface lease 保留到 Released。原生 sample 必须直接暴露系统 DXGI buffer 接口，并以 tracked-sample 最终引用回调释放 mutex；后台 worker 与回调在完整寿命内阻止 DLL 卸载，同步 pipe accept/read 在 Shutdown 时必须可取消并 join。绑定 D3D manager 后不得因 pipe 忙、逐帧失败或代际耗尽回退 CpuBridge；未绑定 manager 时保留独立 CpuBridge。PICQ Shared Frame Ring 必须以不产生 pixel demand 的 checked signal 和槽位 kind 显式区分 Live/Placeholder；GpuNative Live 不激活 CPU pixel reader。初始无 Producer 直接为 Placeholder，短断连 hold 只适用于已观察到的同一 mapping 实时画面。进入 waiting/reconnecting 或断连 hold 到期时清除 retained native frame、切断旧 control channel并推进 native session，只允许为显式 Placeholder 生成自有 CPU sample；最终 sample 准入必须复核同时绑定 mapping epoch 与 content signal 的 live token，信号前已准入的 in-flight sample 不作跨进程撤回，信号后的旧 prepared sample 和 Receiver 重启前旧 mapping sample 均不得入队；恢复 Live 后重建 native 会话，不得无限显示旧实时画面 | wire roundtrip/非法布局、双向 pipe peer identity、无效 HANDLE 拒绝、PICQ 拒绝旧 PICO ABI、control observation 零 CPU demand、初始缺席/短断连/超时、mapping replacement token、显式 Placeholder kind roundtrip、占位清除 cached live、native Fresh/Cached、output/session revision 溢出与幂等、无客户端 teardown 有界测试、Windows Receiver target check、`picoo-frame-hub` / `picoo-gpu` / MF Source all-targets warnings-as-errors；真实 Frame Server 720p/1080p × 30/60、格式切换、重复帧时间戳、waiting/reconnecting 隐私切换、竖屏 letterbox、设备丢失、DLL unload 及会议软件消费另验 |

| REQ-PICOO-VCAM-017 | implemented | ARCH-PICOO-MEDIA-002 / REQ-PICOO-NEXT-013、014、030、032、033、034、036、038 | macOS Camera Extension 同一设备提供公开 `.source` 与仅授权 `com.haoxincode.picoo-camera` Host 的 `.sink`；Host 通过 CMIO Hardware output queue 向精确 legacy UID 的唯一 sink 提交已完成、固定横向 720p/1080p × 30/60 NV12 图像。`GpuNative` 直接提交 Apple renderer 的 IOSurface-backed 目标；显式诊断 `CpuBridge` 只把相同目标逐行物化到独立三槽 IOSurface 池，不从源帧导出。扩展只在 source 客户端实际取流时消费 sink，缓存最后一张兼容图像，由独立有理数 SampleClock 生成客户端时间戳；无图像时输出黑帧。source 格式事务同步 sink，格式不匹配拒绝缓存。Host output queue 固定三槽、单 Producer，持有 sample 到系统移除 token；回调绑定 owner generation，旧 callback 不得释放新 owner 的同地址 sample。waiting/reconnecting 清除 live 待处理结果并以最新 revision 占位替换，最终入队与 revision 在同一锁内线性化；停止时按“停流→注销 callback→确认清队列/reset→释放 owner 引用”顺序执行；任一步失败则保留 owner/in-flight 引用并停止重连，避免迟到 callback 访问已释放状态。App Group 不承载像素数据 | Swift 6 strict-concurrency/warnings-as-errors harness 覆盖 source/sink 双流、共享四格式选择、格式拒绝、SampleClock 与三槽池；Rust AppleCpuBridge 覆盖像素一致、三槽耗尽/释放，CMIO FFI 常量与所有相关 crate all-targets warnings-as-errors；旧 Swift/C Reader 和 Rust macOS App Group ring 已删除。已签名系统扩展的真实 sink 消费、四格式切换、GpuNative/CpuBridge、占位隐私、会议软件与持续吞吐仍留共同验收，不以编译/合成测试替代 |
