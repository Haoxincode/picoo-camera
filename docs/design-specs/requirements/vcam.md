# REQ-PICOO-VCAM：桌面虚拟摄像头

| ID | 状态 | 来源 | 描述 | 验收 |
| --- | --- | --- | --- | --- |
| REQ-PICOO-VCAM-001 | implemented | PUC-004 | 注册统一名称 `Picoo Camera` | `FRIENDLY_NAME` / `PICOO_VCAM_FRIENDLY_NAME`；CI `verify_windows_bundle.ps1` UTF-16 嵌入校验；真机枚举仍待 |
| REQ-PICOO-VCAM-002 | planned | ARCH-PICOO-VCAM-001 | MFCreateVirtualCamera + 独立 Rust IMFMediaSource DLL | 普通启动只通过无注册表写入能力的 `start_registered_on_worker` 启动已安装设备，Session 与 COM/MF 生命周期由专用线程持有；`windows-rs` 实现 COM/MF；`MENewStream`/`MEUpdatedStream`；客户端协商 480p/720p/1080p NV12 后输出类型在运行期保持稳定，Shared Frame Ring 输入由 Source 等比缩放/黑边补齐并缓存转换结果，像素转换不持有 stream COM 状态锁，不在 `RequestSample` 动态重协商；start/stop 失败回滚 allocator 与可见状态；`windows-latest` Cargo 产出 `PicooVirtualCameraSource.dll`；Windows bundle export/load smoke；真机枚举仍待 |
| REQ-PICOO-VCAM-003 | implemented | PUC-004 | VCam 进程只读 Shared Frame Ring，不持有 QUIC/解码器 | Rust VCam Source 只依赖 `picoo-frame-hub` 与 Windows 系统 API；直接复用共享环与占位帧实现 |
| REQ-PICOO-VCAM-004 | implemented | PUC-001 | 安装器注册 COM/MF 组件与卸载清理；系统级修复只由安装器或用户显式操作触发 | WiX `-arch x64` 包 + 声明式 64 位 COM 注册表 + FirewallException QUIC(4433) + 安装/卸载时 WixQuietExec 调用 `--register-vcam --no-wait` / `--unregister-vcam`；CI 读取 MSI PID_TEMPLATE 并要求 `x64;`，防止默认 x86 包落入 `Program Files (x86)` 与 32 位注册表视图；虚拟摄像头页的显式“安装或修复”操作通过 Windows `runas` UAC 边界启动同一维护命令，并在原页面显示等待、成功或失败结果；普通启动与状态检测不写 HKLM、不启动 `reg.exe`、不请求提权；CLSID 三方同步；`validate_wix_scaffold.sh`；CI 钉扎 WiX 5.0.2；修正版 MSI 真机重装仍待 |
| REQ-PICOO-VCAM-005 | proposed | PUC-004 | Zoom/Teams/腾讯会议/OBS/浏览器可选用 | [会议软件验收清单](../verification/vcam-meeting-apps.md)（需 Win11） |
| REQ-PICOO-VCAM-006 | planned | ARCH-PICOO-VCAM-001 | macOS 使用 Core Media I/O Camera Extension 注册 `Picoo Camera` | Swift 6 CMIO 扩展与 `group.com.haoxincode.picoo-camera` mmap 消费边界已可编译；`package macos` 将 `com.haoxincode.picoo-camera.camera-extension.systemextension` 嵌入 Host `.app` 标准目录；Host 通过纯 Rust `objc2-system-extensions` 在后台查询、激活与停用，严格按 Bundled/待批准/重启后生效/移除中/Active 限定操作，并持久化跨重启 intent；仅更高 build 可替换；检测中与推流均不得冒充 Active；仍需已签名真机上的用户批准、重启枚举、移除和会议软件验收；扩展不持有 QUIC/Decoder |
| REQ-PICOO-VCAM-007 | planned | ARCH-PICOO-VCAM-001 / ci-and-build.md | macOS 发布产物满足 Hardened Runtime、Developer ID 签名与公证 | `cargo xtask release macos` 强制递增 release/build 版本并同步 Host/Extension；校验 Developer ID profile 的有效期、分发类型、Team/Bundle/App Group/System Extension capability 与授权证书，按内→外启用 Hardened Runtime/secure timestamp 签名，再复核实际 signer 和 effective entitlements，最后执行 `notarytool`、staple、`codesign --deep --strict` 与 `spctl`；release workflow 的 action 固定 SHA、凭据文件权限收紧并 always 清理；仍需凭据配置后的首次远端 release 绿测和签名真机验收 |
| REQ-PICOO-VCAM-008 | planned | ARCH-PICOO-VCAM-001 | Windows MF Source 可观测每秒 RequestSample、fresh、cached、placeholder、failed 与 delivery time；每个 request 与 outcome 必须原子归入同一统计窗口；是否增加 pacing 必须由 Frame Server 真机指标决定，不能把外部项目的同步 sleep 直接作为平台规则 | Windows debug/诊断计数 + Teams/Zoom/OBS/浏览器真机记录；声明 30 FPS 时不得出现持续异常 request pump |
